import type Item from './Item';
import type Metadata from './Metadata';
import type PageViewport from './parse/PageViewport';
export default class ParseResult {
    fontMap: Map<string, object>;
    pageCount: number;
    pdfjsPages: any[];
    pageViewports: PageViewport[];
    metadata: Metadata;
    schema: string[];
    items: Item[];
    constructor(fontMap: Map<string, object>, pageCount: number, pdfjsPages: any[], pageViewports: PageViewport[], metadata: Metadata, schema: string[], items: Item[]);
}
