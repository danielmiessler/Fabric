import GlobalDefinition from './GlobalDefinition';
export default class GlobalValue<T> {
    definition: GlobalDefinition<T>;
    value: T;
    override: boolean;
    constructor(definition: GlobalDefinition<T>, value: T, override?: boolean);
}
